apiVersion: v1
kind: Secret
metadata:
  name: ecr-credentials
  namespace: argocd
type: kubernetes.io/dockerconfigjson
data:
  # This is a template - the actual .dockerconfigjson will be generated by the script
  # To generate: kubectl create secret docker-registry ecr-credentials \
  #   --docker-server=488363440930.dkr.ecr.ap-northeast-1.amazonaws.com \
  #   --docker-username=AWS \
  #   --docker-password=$(aws ecr get-login-password --region ap-northeast-1) \
  #   -n argocd --dry-run=client -o yaml
  .dockerconfigjson: ""