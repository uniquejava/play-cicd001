name: CD to Development

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed
    branches: [ develop ]

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    name: Deploy to Development (Disabled)
    environment: development

    steps:
    - name: Skip deployment (placeholder)
      run: |
        echo "🚫 Development deployment is temporarily disabled"
        echo "📝 CD pipeline is paused - no infrastructure available"
        echo "✅ Workflow completed successfully (no-op)"